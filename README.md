# Heavy-tail-behavior-of-daily-returns
This project analyzes the heavy‑tail behavior of daily log returns for a few stocks. We compute log returns, plot their distribution, and estimate the tail index for each stock.
